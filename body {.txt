body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    margin: 20px;
    background-color: #f4f4f4;
    color: #333;
}
.quiz-container {
    background: #fff;
    padding: 30px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
    max-width: 800px;
    margin: auto;
}
.section-title {
    font-size: 24px;
    margin-bottom: 20px;
    text-align: center;
}
.passage {
    background-color: #e9f7ef;
    border-left: 5px solid #28a745;
    padding: 15px;
    margin-bottom: 30px;
    border-radius: 4px;
}
#quizForm {
    display: flex;
    flex-direction: column; /* Stack questions vertically */
    gap: 20px; /* Space between question blocks */
}
.question-block {
    width: 100%; /* Each question block takes full width */
    box-sizing: border-box;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 15px;
    background-color: #fdfdfd;
}
.question-number {
    font-weight: bold;
    font-size: 1.1em;
    margin-bottom: 10px;
}
.options {
    display: flex; /* Flexbox for options within a question */
    flex-wrap: wrap; /* Allow options to wrap if content is long */
    gap: 10px; /* Space between options A, B, C, D */
    justify-content: space-between; /* Distribute options evenly */
}
.options label {
    flex: 1 1 calc(25% - 15px); /* Four options per row, adjusting for gap */
    min-width: 150px; /* Minimum width for each option to prevent crushing */
    box-sizing: border-box;
    cursor: pointer;
    padding: 10px;
    border: 1px solid #eee;
    border-radius: 4px;
    transition: background-color 0.3s ease, border-color 0.3s ease; /* Add border-color to transition */
    text-align: center;
    background-color: #fff;
}
.options label:hover {
    background-color: #e9e9e9;
}
.options input[type="radio"] {
    display: none; /* Hide the default radio button */
}
/* Styles for correct/incorrect answers */
.correct {
    background-color: #d4edda; /* Light green */
    border-color: #28a745; /* Green */
}
.incorrect {
    background-color: #f8d7da; /* Light red */
    border-color: #dc3545; /* Red */
}

.submit-btn {
    display: block;
    width: 180px;
    padding: 12px 25px;
    margin: 30px auto 0;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 18px;
    text-align: center;
    transition: background-color 0.3s ease;
}
.submit-btn:hover {
    background-color: #0056b3;
}
.result {
    margin-top: 20px;
    font-size: 20px;
    font-weight: bold;
    text-align: center;
    color: #007bff;
    padding: 10px;
    background-color: #e2f0ff;
    border-radius: 5px;
    display: none; /* Hidden by default */
    width: fit-content;
    margin-left: auto;
    margin-right: auto;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
    .options label {
        flex: 1 1 calc(50% - 10px); /* Two options per row on medium screens */
    }
}
@media (max-width: 480px) {
    .options label {
        flex: 1 1 100%; /* One option per row on small screens */
    }
}